<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
    "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.robi.mapper.RecordsMapper">
    <select id="selectById" parameterType="Long" resultType="com.robi.data.dao.RecordsDao">
        <!-- ... -->
    </select>
    <select id="selectAllByWriteUserId" parameterType="Long" resultType="com.robi.data.dao.RecordsDao">
        <!-- ... -->
    </select>
    <insert id="insert" parameterType="com.robi.data.dao.RecordsDao">
        <!-- ... -->
    </insert>
    <update id="update" parameterType="com.robi.data.dao.RecordsDao">
        <!-- ... -->
    </update>
    <delete id="deleteById" parameterType="Long">
        <!-- ... -->
    </delete>
</mapper>